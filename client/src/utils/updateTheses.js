export const updateTheses = (answers) => {
    // Your logic here to update theses based on answers
    // Example: If a certain answer implies atheism, update the atheism thesis
    if (1 === 1 ||
        2 === 2 ) {
        answers['thesissoundargumentforatheism'] = '1'; // Use a string or consistent format for answers
    }
    // Other conditions that affect theses
    return answers;
};